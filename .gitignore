# Exercise media — fetched on first `docker compose up` (or via scripts/fetch-media.sh)
media/img/
media/gif/

# Runtime data — users, passkeys, per-user state, session secret
data/

# Local backups may contain every user's data and provider credentials
backups/

# Cloudflare Tunnel configuration and credentials belong only to the host
cloudflared/

# Local config
.env

# Build output / deps
frontend/dist/
node_modules/
*.log
.DS_Store

# Local AI-assistant tooling — contributor's own setup, not part of the app
.claude/
.codex/
CLAUDE.md
graphify-out/
